export const muiStyles = theme => ({
    typography: {
        maxWidth: '100%',
        color: theme.palette.primary.contrastText,
        display: 'inline',
    },
    routerLinkSpy: {
        cursor: 'default',
    },
})
